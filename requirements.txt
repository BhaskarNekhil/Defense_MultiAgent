# ── Web server (existing) ────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0
openai>=1.30.0

# ── Qwen / HuggingFace inference ─────────────────────────────────────────────
transformers>=4.45.0
accelerate>=0.30.0
sentencepiece>=0.2.0
tokenizers>=0.19.0

# ── PEFT / LoRA adapters ──────────────────────────────────────────────────────
peft>=0.12.0

# ── TRL — GRPO trainer (Group Relative Policy Optimisation) ──────────────────
trl>=0.11.0

# ── PyTorch (CPU default; install CUDA wheel separately if GPU available) ────
# GPU: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0

# ── 4-bit / 8-bit quantisation (GPU only — safe to skip on CPU) ──────────────
# bitsandbytes>=0.43.0   # uncomment for GPU quantisation

# ── Data / utilities ─────────────────────────────────────────────────────────
numpy>=1.26.0
datasets>=2.20.0
tqdm>=4.66.0
